{
  "name": "taylor-reach",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @taylor-reach/dashboard dev",
    "build": "pnpm --filter @taylor-reach/dashboard build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
